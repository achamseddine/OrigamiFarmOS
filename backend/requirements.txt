fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
alembic>=1.13
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
# passlib 1.7.4 probes bcrypt with a >72-byte secret; bcrypt 4.1+ raises on
# that instead of truncating, and every password hash fails at import.
bcrypt>=4.0,<4.1
python-multipart>=0.0.9
psycopg2-binary>=2.9 ; python_version >= "3.9"

# Dev / test
pytest>=8.0
httpx>=0.27
