<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Signing in, syncing, and every API call need this, and a release
         build gets it from here or not at all: Flutter injects INTERNET
         into the debug and profile manifests for hot reload, so a missing
         declaration here works perfectly on a developer's machine and
         fails on every installed APK. It fails invisibly too — the socket
         is refused inside the process, so the request never reaches the
         network, the server logs nothing, and the app can only report
         that it could not reach the server. Chrome on the same tablet
         works throughout, because the permission is per-app.

         The app is still usable offline afterwards; this is what makes
         the first sign-in, and every later sync, possible at all. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Lets the app tell "no signal" apart from "server is down" rather
         than treating every failure as the latter. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:label="Origami FarmOS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
