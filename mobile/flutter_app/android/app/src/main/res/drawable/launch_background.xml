<?xml version="1.0" encoding="utf-8"?>
<!-- What the window shows between tap and first Flutter frame: the
     Origami mark on paper (or on the pack's near-black at night). The
     mark is the pack's origami-farmos-mark.png, scaled per density. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/splash_mark" />
    </item>
</layer-list>
